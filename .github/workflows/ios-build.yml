name: Build iOS App

on:
  push:
    branches: [ios-app]
  workflow_dispatch:

jobs:
  build:
    runs-on: macos-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Select Xcode 26.5
        run: sudo xcode-select -s /Applications/Xcode_26.5.app/Contents/Developer

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: 'npm'

      - name: Install Capacitor (iOS-only deps, skip server)
        run: |
          npm install \
            @capacitor/core@^8.4.1 \
            @capacitor/cli@^8.4.1 \
            @capacitor/ios@^8.4.1 \
            @capacitor/camera@^8.2.1 \
            @capacitor/filesystem@^8.1.2 \
            @capacitor/haptics@^8.0.2 \
            @capacitor/push-notifications@^8.1.1 \
            @capacitor/splash-screen@^8.0.1 \
            @capacitor/status-bar@^8.0.2 \
            @capacitor/assets

      - name: Generate native iOS project
        run: |
          # Build www/ directory with app files (bundled shell)
          mkdir -p www
          cp index.html www/
          cp *.js www/
          cp *.css www/
          cp -r assets www/
          cp favicon.png www/
          cp *.svg www/ 2>/dev/null || true
          echo "www/ ready"
          npx cap add ios
          npx cap sync ios

      - name: Add privacy strings to Info.plist
        run: |
          /usr/libexec/PlistBuddy -c "Add :NSPhotoLibraryUsageDescription string 'Align PM uses your photo library to attach images to daily logs, punchlists, and project photos.'" ios/App/App/Info.plist 2>/dev/null || /usr/libexec/PlistBuddy -c "Set :NSPhotoLibraryUsageDescription 'Align PM uses your photo library to attach images to daily logs, punchlists, and project photos.'" ios/App/App/Info.plist
          /usr/libexec/PlistBuddy -c "Add :NSCameraUsageDescription string 'Align PM uses the camera to capture photos for daily logs, punchlists, and project documentation.'" ios/App/App/Info.plist 2>/dev/null || /usr/libexec/PlistBuddy -c "Set :NSCameraUsageDescription 'Align PM uses the camera to capture photos for daily logs, punchlists, and project documentation.'" ios/App/App/Info.plist
          /usr/libexec/PlistBuddy -c "Add :NSPhotoLibraryAddUsageDescription string 'Align PM saves project photos to your library.'" ios/App/App/Info.plist 2>/dev/null || /usr/libexec/PlistBuddy -c "Set :NSPhotoLibraryAddUsageDescription 'Align PM saves project photos to your library.'" ios/App/App/Info.plist

      - name: Write export options
        run: |
          cat > ios/App/exportOptions.plist << 'PLIST'
          <?xml version="1.0" encoding="UTF-8"?>
          <!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
          <plist version="1.0">
          <dict>
              <key>method</key>
              <string>app-store-connect</string>
              <key>teamID</key>
              <string>${{ secrets.APPLE_TEAM_ID }}</string>
              <key>signingStyle</key>
              <string>automatic</string>
              <key>uploadBitcode</key>
              <false/>
              <key>uploadSymbols</key>
              <true/>
          </dict>
          </plist>
          PLIST

      - name: Generate app icon assets
        run: |
          test -f assets/icon-only.png
          npx capacitor-assets generate \
            --ios \
            --iconBackgroundColor '#1B2A3D' \
            --iconBackgroundColorDark '#1B2A3D' \
            --assetPath assets/
          test -s ios/App/App/Assets.xcassets/AppIcon.appiconset/AppIcon-512@2x.png

      - name: Write App Store Connect API key
        run: |
          mkdir -p ~/private_keys
          echo "${{ secrets.APP_STORE_CONNECT_API_KEY }}" > ~/private_keys/AuthKey_${{ secrets.APP_STORE_CONNECT_KEY_ID }}.p8
          chmod 600 ~/private_keys/AuthKey_${{ secrets.APP_STORE_CONNECT_KEY_ID }}.p8

      - name: Setup keychain for signing
        run: |
          security create-keychain -p temp123 signing.keychain
          security default-keychain -s signing.keychain
          security unlock-keychain -p temp123 signing.keychain
          security set-keychain-settings -t 3600 -l ~/Library/Keychains/signing.keychain-db
          # Automatic signing via API key — no pre-imported certs needed

      - name: Build and archive
        run: |
          cd ios/App
          xcodebuild archive \
            -project App.xcodeproj \
            -scheme App \
            -sdk iphoneos \
            -destination 'generic/platform=iOS' \
            -archivePath ./build/App.xcarchive \
            -configuration Release \
            CODE_SIGN_STYLE=Auto \
            DEVELOPMENT_TEAM=${{ secrets.APPLE_TEAM_ID }} \
            CURRENT_PROJECT_VERSION=${{ github.run_number }} \
            -allowProvisioningUpdates \
            -authenticationKeyID ${{ secrets.APP_STORE_CONNECT_KEY_ID }} \
            -authenticationKeyIssuerID ${{ secrets.APP_STORE_CONNECT_ISSUER_ID }} \
            -authenticationKeyPath ~/private_keys/AuthKey_${{ secrets.APP_STORE_CONNECT_KEY_ID }}.p8

      - name: Export .ipa
        run: |
          cd ios/App
          xcodebuild -exportArchive \
            -archivePath ./build/App.xcarchive \
            -exportOptionsPlist exportOptions.plist \
            -exportPath ./build \
            -allowProvisioningUpdates \
            -authenticationKeyID ${{ secrets.APP_STORE_CONNECT_KEY_ID }} \
            -authenticationKeyIssuerID ${{ secrets.APP_STORE_CONNECT_ISSUER_ID }} \
            -authenticationKeyPath ~/private_keys/AuthKey_${{ secrets.APP_STORE_CONNECT_KEY_ID }}.p8

      - name: Upload to App Store Connect
        run: |
          xcrun altool --upload-app \
            -f ios/App/build/App.ipa \
            -t ios \
            -u "${{ secrets.APPLE_ID }}" \
            -p "${{ secrets.APPLE_APP_PASSWORD }}"

      - name: Upload .ipa artifact
        uses: actions/upload-artifact@v4
        with:
          name: Align-PM.ipa
          path: ios/App/build/App.ipa
